volumes:
  dj_template_local_postgres_data: {}
  dj_template_local_postgres_data_backups: {}
  

services:
  django:
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: dj_template_local_django
    container_name: dj_template_local_django
    depends_on:
      - postgres
    volumes:
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: dj_template_production_postgres
    container_name: dj_template_local_postgres
    volumes:
      - dj_template_local_postgres_data:/var/lib/postgresql/data
      - dj_template_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres
